export type UserProfile = {
  userId: string;
  displayName: string;
  avatarEmoji: string;
};

export type ShoppingListSummary = {
  id: string;
  name: string;
  shareCode: string;
  isArchived: boolean;
};
