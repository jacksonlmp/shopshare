# Backend API (browser calls this host; use localhost when API is published on host port 8000)
VITE_API_BASE_URL=http://localhost:8000

# Optional: override WebSocket origin (default: derived from VITE_API_BASE_URL)
# VITE_WS_BASE_URL=ws://localhost:8000
